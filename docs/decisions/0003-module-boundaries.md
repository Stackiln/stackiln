# 0003: Modules are copied source

A module owns explicit files, routes, schema surfaces, environment names, permissions, and events. The planner resolves dependencies, checks cycles and collisions, and copies only enabled recipes. Navigation and the public sitemap are generated from enabled module metadata. Generated products have no runtime dependency on StackKiln. Advanced module operations remain pending.
